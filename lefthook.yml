# EXAMPLE USAGE:
#
#   Refer for explanation to following link:
#   https://lefthook.dev/configuration/
#
pre-commit:
  parallel: true
  commands:
    lint:
      run: pnpm eslint --fix {staged_files}
      glob: "*.{js,mjs,ts,jsx,vue}"
      stage_fixed: true
